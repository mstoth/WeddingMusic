{% extends "base_generic.html" %}

{% block content %}
  <h1>Composer List</h1>
  {% if piece_list %}
  <ul>
    {% for composer in composer_list %}
      <li>
        <a href="{{ composer.get_absolute_url }}">{{ composer.title }}</a> ({{composer.composer}})
      </li>
    {% endfor %}
  </ul>
  {% else %}
    <p>There are no composers in the library.</p>
  {% endif %} 
{% endblock %}
